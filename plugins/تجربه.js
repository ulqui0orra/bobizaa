let handler = m => m 
 handler.all = async function (m) { 
 let chat = global.db.data.chats[m.chat] 
  
 if (/^اعرص$/i.test(m.text) ) { //sem prefixo 
     conn.reply(m.chat, `.طرد`, m) //wm, null, [['Menu', '#menu']], m) botones :V 
  
 } 
  
 if (/^خول$/i.test(m.text) ) { //sem prefixo 
     conn.reply(m.chat, `.طرد`, m) //wm, null, [['Menu', '#menu']], m) botones :V 
  
 } 
  
 if (/^لبوه$/i.test(m.text) ) { //sem prefixo 
     conn.reply(m.chat, `.طرد`, m) //wm, null, [['Menu', '#menu']], m) botones :V 
  
 } 
  
 if (/^شرموط$/i.test(m.text) ) { //sem prefixo 
     conn.reply(m.chat, `.طرد`, m) //wm, null, [['Menu', '#menu']], m) botones :V 
  
 } 
  
 if (/^متناك$/i.test(m.text) ) { //sem prefixo 
     conn.reply(m.chat, `.طرد`, m) //wm, null, [['Menu', '#menu']], m) botones :V 
  
 } 
  
 if (/^شاذ$/i.test(m.text) ) { //sem prefixo 
     conn.reply(m.chat, `.طرد`, m) //wm, null, [['Menu', '#menu']], m) botones :V 
  
 } 
  
 if (/^كلب|كلبب|كلببب$/i.test(m.text) ) { //sem prefixo 
     conn.reply(m.chat, `.طرد`, m) //wm, null, [['Menu', '#menu']], m) botones :V 
  
 } 
  
 if (/^خخخخخخخخ$/i.test(m.text) ) { //sem prefixo 
     conn.reply(m.chat, `.طرد`, m) //wm, null, [['Menu', '#menu']], m) botones :V 
  
 } 
  
 if (/^طيز|زب|كس/i.test(m.text) ) { //sem prefixo 
     conn.reply(m.chat, `.طرد`, m) //wm, null, [['Menu', '#menu']], m) botones :V 
  
 }  
  
 if (/^مومس$/i.test(m.text) ) { //sem prefixo 
     conn.reply(m.chat, `.طرد`, m) //wm, null, [['Menu', '#menu']], m) botones :V 
  
 } 
     
 if (/^زاني$/i.test(m.text) ) { //sem prefixo 
     conn.reply(m.chat, `.طرد`, m) //wm, null, [['Menu', '#menu']], m) botones :V 
  
 } 
  
  if (/^زانيه$/i.test(m.text) ) { //sem prefixo 
     conn.reply(m.chat, `.طرد`, m) //wm, null, [['Menu', '#menu']], m) botones :V 
  
 } 
   
 return !0 } 
 export default handler
